// Q-Caliper DOE 实验设计报告模板

#import "template.typ": project

#show: project.with(
  title: "实验设计 (DOE) 分析报告",
  subtitle: "由 Q-Caliper 自动生成 | 模块: {{MODULE}}"
)

= 1. 实验设计总结

本章节汇总了本次实验设计的基本参数与配置信息。

{{TABLE_DATA_SUMMARY}}

{{DESIGN_TYPE_INFO}}

= 2. 设计矩阵

下表展示了完整的实验设计矩阵, 包含各次实验运行中各因子的编码水平设置。

{{TABLE_DESIGN_MATRIX}}

= 3. 因子效应分析

采用普通最小二乘法 (OLS) 回归模型对因子效应进行估计。下表列出了各因子的效应值、回归系数、t 统计量和 p 值。

{{TABLE_EFFECTS}}

模型整体统计量:

{{TABLE_MODEL_STATS}}

= 4. 图表分析

以下图表展示了各因子的主效应和 Pareto 排序结果。

{{CHARTS}}

= 5. 指标诊断与原理解释

== 5.1 因子效应计算原理

因子效应 (Factor Effect) 衡量的是某因子从低水平 (-1) 变化到高水平 (+1) 时, 响应变量的平均变化量。

- *效应值 (Effect)*: $E_i = bar(y)_(+) - bar(y)_(-)$, 其中 $bar(y)_(+)$ 和 $bar(y)_(-)$ 分别为该因子在高水平和低水平时的响应均值。
- *回归系数 (Coefficient)*: 效应值的一半, 即 $b_i = E_i \/ 2$。回归模型为 $y = b_0 + sum b_i x_i$。
- *t 统计量*: $t_i = b_i \/ "SE"(b_i)$, 用于检验该因子效应是否显著异于零。
- *p 值*: 在原假设 (效应为零) 下, 观测到当前或更极端 t 值的概率。p < 0.05 表示效应在 95% 置信水平下显著。

== 5.2 Pareto 排序说明

Pareto 效应排序图将各因子按绝对效应值从大到小排列, 帮助快速识别对响应影响最大的关键因子。

- 柱状图颜色区分: 红色表示 p < 0.05 的显著因子, 灰色表示不显著因子。
- 红色虚线为显著性阈值线, 基于 t 分布的临界值计算: $"阈值" = t_(alpha\/2, "df"_"resid") times sqrt(4 "MSE" \/ n)$。

== 5.3 显著性判定规则

本报告采用 $alpha = 0.05$ 作为显著性判定标准:

- *p < 0.05*: 因子效应显著, 对响应变量有统计学意义上的影响。
- *0.05 <= p < 0.10*: 因子效应边际显著, 可能存在影响但证据不够充分。
- *p >= 0.10*: 因子效应不显著, 可认为该因子对响应无实质性影响。

== 5.4 模型统计量解读

- *R²*: 决定系数, 表示模型解释的变异占总变异的比例。R² 越接近 1, 模型拟合越好。
- *R²-adjusted*: 调整后的 R², 对模型中自变量个数进行惩罚, 避免过拟合。
- *F 统计量*: 模型整体显著性检验。F 值越大且 p 值越小, 说明模型整体越显著。
