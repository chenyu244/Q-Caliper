// Q-Caliper MSA 测量系统分析报告模板 (Minitab 风格)

#import "template.typ": project

#show: project.with(
  title: "测量系统分析 (MSA) 报告",
  subtitle: "由 Q-Caliper 自动生成 | 模块: {{MODULE}}"
)

= 1. 偏差分析 (Bias Analysis)

== 1.1 数据总结

{{TABLE_BIAS_SUMMARY}}

== 1.2 分析结果

采用单样本 t 检验对量具偏差进行统计检验。

{{TABLE_BIAS_RESULT}}

== 1.3 判定标准 (偏差分析)

#table(
  columns: (auto, auto, auto),
  inset: 8pt,
  stroke: 0.5pt,
  table.header([条件], [结论], [行动]),
  [p 值 >= 0.05], [无显著偏差], [偏差分析通过, 量具准确],
  [p 值 < 0.05], [存在显著偏差], [重新校准或调整量具],
  [95% CI 包含 0], [偏差在可接受范围], [偏差分析通过],
  [95% CI 不包含 0], [偏差超出可接受范围], [调查偏差来源并纠正],
)

= 2. 线性分析 (Linearity Analysis)

== 2.1 数据总结

{{TABLE_LINEAR_SUMMARY}}

== 2.2 分析结果

采用最小二乘线性回归拟合偏差与参考值的关系。

{{TABLE_LINEAR_RESULT}}

各参考点明细:

{{TABLE_LINEAR_POINTS}}

== 2.3 过程变异 (Process Variation) 说明

过程变异 (PV) 是制造过程输出的总波动宽度, 用于计算 P/T 比。本工具按以下优先级自动计算:

+ *有规格限 (USL/LSL)*: PV = USL - LSL。这是 AIAG MSA 手册推荐的首选方法, 反映客户要求的公差范围, 不随过程波动变化。
+ *无规格限*: PV = 6 #sym.times 全样本标准差。反映实际过程表现, 但要求过程受控且数据覆盖完整量程。

线性分析根据 PV 计算 P/T 比 (P/T = |slope| #sym.times PV), 评估线性偏差占过程变异的比例。

== 2.4 判定标准 (线性分析)

#table(
  columns: (auto, auto, auto),
  inset: 8pt,
  stroke: 0.5pt,
  table.header([条件], [结论], [行动]),
  [P/T 比 < 0.1], [线性可接受], [量具在整个量程内偏差一致],
  [P/T 比 0.1 - 0.3], [线性有条件接受], [根据应用重要性决定是否改善],
  [P/T 比 > 0.3], [线性不可接受], [量具需要维护或更换],
  [R-squared 接近 1], [线性关系显著], [偏差随参考值线性变化],
  [R-squared 接近 0], [无线性趋势], [偏差与参考值无关],
)

= 3. 统计图表与数据分布

{{CHARTS}}

= 4. 指标诊断与原理解释

== 4.1 偏差分析说明
偏差 (Bias) 是观测均值与参考值 (标称真值) 之间的系统性差异:
- *原假设 (H0)*: 观测均值等于参考值 (无偏差)
- *备择假设 (H1)*: 观测均值不等于参考值 (存在偏差)
- 若 *p 值 < 0.05*, 拒绝原假设, 认为量具存在显著偏差
- *95% 置信区间*给出了偏差真实值的估计范围

偏差的常见原因: 量具校准偏移、基准件磨损、环境温度影响、操作者系统性读数偏差等。

== 4.2 线性分析说明
线性分析评估量具在预期工作范围内的偏差是否随测量值大小而变化:
- *斜率 (Slope)*: 反映偏差随参考值变化的速率。理想值为 0 (无比例偏差)
- *截距 (Intercept)*: 反映固定偏差。理想值为 0
- *R-squared*: 回归拟合优度, 越接近 1 说明线性关系越强
- *P/T 比*: 斜率绝对值与过程变异的乘积, 用于评估线性偏差对过程能力的影响
