# To-Do List

## 1. 数据处理与智能推荐功能完善
- [ ] **增强的数据类型清洗**: 在获取用户手动编辑的新数据保存到 `[Q]_` 副本时，确保单元格内的数据类型（尤其是纯数字字符串 vs. 浮点数）被 pandas 准确推断，防止写入 Excel 时带有类型警告（如绿三角）。
- [ ] **智能跳转的动态参数传递**:
  - 当前“智能分析”按钮调用的 `self._goto_analysis` 是直接硬编码切换至 CPK 面板。
  - 后续需要根据推断出来的顶部角色表头（例如如果只包含一列"测量值"则推荐去 SPC 或 CPK；若包含"操作者"和"零件"则推荐去 GRR），并自动将角色映射传递给跳转后的子面板。
- [ ] 报告增加日期时间尾缀，如当前位置有同名文件，自动追加版本尾缀ABC...

## 2. DOE迭代
- [ ] 更合适的教程操作
- [ ] 类似其他模块的报告输出

## 3. 报告风格的模版精细调整
  - 表格
  - 标题后行距加大