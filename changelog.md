# Changelog (2026-05-07)

## 数据中心模块 (Data Center)
- **智能推荐引擎**:
  - 新增基于列角色分配的动态推荐逻辑。当识别到“测量值”时推荐“正态分析”；同时识别到“操作者”与“零件”时推荐“GRR 分析”。
  - **跨页面数据流**: 点击智能推荐跳转时，会自动将数据中心的列映射信息带入目标分析面板，实现无缝衔接。
  - **UI 术语同步**：将全链路的“Cpk 分析”统一更名为更为通用的“正态分析”。

## 正态分析模块 (Normal Analysis / 原 CPK)
- **UI 布局深度重构**:
  - 废弃了左参数右图表的旧布局，改为更符合操作直觉的“上参数（QGridLayout 完美对齐）+ 下图表”垂直结构。
  - 移除了原有的独立结果表格，将核心指标（Cp/Pp、Cpk/Ppk、均值、标准差）以 Minitab 风格直接集成在图表右侧的统计信息框中，并支持左右并排对比。
- **智能参数填充**:
  - 新增规格限“智能推荐”下拉菜单，支持基于当前数据一键填充 ±3 Sigma、±6 Sigma 或全范围 (Max/Min)。
  - 子组大小默认值调整为行业推荐值 5，并附带了详尽的悬停帮助提示（问号图标）。
- **算法核心增强 (Core Engine)**:
  - 核心引擎 `calculate_capability` 现已支持无规格限 (USL/LSL) 输入下的纯基础正态性分析。
  - 引入了工业级的预期不良率 (PPM) 预估算法，包含实际观测值、预期组内缺陷率和预期整体缺陷率。

## 报告引擎与 Typst 深度集成 (Report Engine)
- **沙箱隔离与图表引用**:
  - 彻底解决了 Typst `root` 沙箱机制导致的外部图片无法引用的报错。现在的报告引擎会在后台建立独立的隔离工作区，统一拷贝模板与图片，确保编译稳定。
- **统一版式与彻底 Typst 化**:
  - 全面废弃了 Python 代码中错误生成的 Markdown 表格语法，重写为原生的 Typst `#table` 语法结构。
  - 所有子模块的报告模板现已全部挂载并继承自用户指定的统一基础模板 (`docs/template.typ`)，保证了企业级文档风格的高度统一。
- **正态能力报告专业化**:
  - `cpk_report.typ` 升级为对标 Minitab 的五章式专业报告：包含基础数据、正态性检验结论、过程能力与 PPM 性能、图表分布，以及深度原理解释（提供关于 Cpk/Ppk 漂移的工程诊断建议）。
  - 通过 `custom_data` 将精准化格式化的 Typst 代码直接动态注入到报告的对应占位符中。

## Bug 修复与稳定性 (Bug Fixes)
- **UI 图标修复**: 修复了 `cpk_panel.py` 中由于多次误用不存在的 `FluentIcon` 属性（如 `TIER`, `LIGHT_BULB`, `QUICK_CONTROL`）导致的启动崩溃，统一替换为标准图标。
- **防止卡死**: 修复了 PDF 导出错误弹窗阻塞主线程导致的界面卡死问题，改用非阻塞式 `InfoBar` 长效显示。
- **排版字符修复**: 修复了 Matplotlib 统计框中文字符显示为方框的问题；去除了失效的 markdown 加粗控制符 `*`，并避免其与 Typst 表格渲染逻辑产生冲突而引发 `unclosed delimiter` 异常。
- **测试覆盖**: 将后台测试脚本规范化，重建为 `q_caliper/tests/test_report.py` 以便后期持续调试 Typst 编译。

# Changelog (2026-05-04)

## 数据中心模块深度优化 (Data Center Enhancements)
- **交互 Bug 全面修复**:
  - **表头角色修改**: 修复了双击表头无效的问题，现支持通过下拉菜单修改列的数据角色（测量值、操作者等），并优化了同步逻辑。
  - **自动保存 (Auto-Sync)**: 修复了加载状态锁死导致的信号丢失问题，现在修改单元格或列名后会自动、静默地回写至 `[Q]_` 前缀的 Excel/CSV 文件。
- **数据安全与健壮性**:
  - **防截断机制**: 彻底解决了原同步逻辑仅保存预览行（100行）的 Bug，通过维护全量数据副本确保文件完整性。
  - **覆盖保护**: 新增源文件导入校验。若文件夹中已存在分析副本，系统会弹出警告并要求确认，防止误操作覆盖已有进度。
- **视觉反馈升级**:
  - **同步时间显示**: 在界面右上角新增“同步时间”标签，方便实时确认后台自动保存的完成状态。
  - **静默交互**: 优化了弹窗提示逻辑，将频繁的自动保存操作改为静默进行，显著提升了连续录入的体验。

## 过程能力引擎深度演进 (Capability Engine)
- **算法接口大一统**: 
  - 重构了 `calculate_capability` 接口，改为“一次调用，全指标返回”模式。无论何种采样配置，均同步返回 Cpk、Ppk 和 Cmk，极大简化了上层调用逻辑。
  - **精度飞跃**: 废弃了子组大小 $n > 10$ 时的近似公式，改为内置覆盖至 $n=25$ 的高精度查表法（基于 ASTM 标准），确保在复杂抽样下的算法权威性。
- **UI 与报告全适配**:
  - `CpkPanel` 结果表格新增 `Cmk` 展示，支持设备能力与过程能力的直观对比。
  - PDF 报告引擎同步更新，确保导出报告中包含完整的分析指标体系。
- **测试保障**: 全面增强了 `test_cpk.py`，覆盖了子组自动切换逻辑及边界样本量检查。

## 文档规范与排版优化 (Documentation)
- **Typst 算法指南重构**:
  - 修正了关于 I-MR 方法与全样本标准差法在工程应用上的逻辑定义错误。
  - **排版升级**: 调整了 `template.typ` 的全局行间距与对齐方式，技术文档视觉体验更显专业。
  - **铁律确立**: 在根目录建立 `GEMINI.md`，确立了 Typst 编写规范，彻底杜绝了 LaTeX 语法的误用。

# Changelog (2026-05-01)

## 核心算法重构 (Core Algorithm)
- **CPK/PPK 计算引擎**: 
  - 移除了旧版废弃的 `calculate_cpk` 接口，全面采用具有统一逻辑和三类分析模式 ("equipment", "subgrouped", "individual") 的 `calculate_capability` 新接口。
  - 重写了 `q_caliper/core/cpk.py` 中的英文注释为中文，方便国内开发者阅读。
  - **精度优化**: 在 `_d2_constant` 中，对于子组大小 $n > 10$ 的情况，舍弃了固定的保守估计 $3.0$，采用更高精度的数学公式 $d_2(n) \approx \sqrt{\frac{\pi n}{2n - 1}}$。

## 文档与排版 (Documentation & Typography)
- **Typst 技术文档修复**:
  - 修复了由于不兼容的 Markdown 表格和旧版 LaTeX 语法导致的严重编译报错。
  - 修正了由于缺少字体导致的全局文本加粗渲染异常，更换为通用的 Windows 字体组合（Times New Roman, SimSun, Microsoft YaHei, Consolas）。
  - **模板化**: 抽取了所有的 `#set` 与 `#show` 样式配置规则至 `docs/template.typ`，并在 `qcaliper_capability_guide.typ` 中进行了重构引用。

## 构建脚本优化 (Build Script)
- **Nuitka 打包精简**:
  - 修复了通过 Nuitka 编译独立 EXE 文件时图标丢失的问题，补充了 `--windows-icon-from-ico=images/Q-Caliper.ico` 参数。
  - 移除了不再必须额外打包的资源文件夹（`data/examples` 与 `images`），以及显式的 `--include-package`，结合 `--follow-imports` 大幅缩减最终产物的体积。

## 界面与 UI 调整 (User Interface)
- **图表视觉精细化**:
  - 统一遍历并修改了 `q_caliper/ui/` 目录下（CPK, MSA, SPC, GRR, DOE）所有基于 matplotlib 的绘图脚本。将过于粗重的曲线（`linewidth=2` 或 `1.5`）全部调细至 `1.2` 或 `1.0`，提升整体美感。
- **数据中心 (Data Center) 全面重构**:
  - 废弃了原本占据空间巨大的独立 `DropZone`，直接为表格 `DataPreviewWidget` 提供全局拖拽支持。
  - **前缀保护**: 导入数据时如果文件名未带 `[Q]_` 前缀，则自动在同目录下拷贝一份带前缀的工作副本，后续所有修改基于副本进行。
  - **智能清洗**: 自动识别并提取前 20 行中的有效表头行，并剔除全空的冗余数据行和列 (`dropna(how="all")`)。
  - **表头下放**: 将 Excel 的真实表头作为数据呈现于界面的第 0 行，允许直接双击编辑。原本顶部的灰色固定表头被改造为“智能角色推断”栏。