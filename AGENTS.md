# Q-Caliper 铁律

## 强制中文交互

- 在任何情况下，系统必须使用 **中文** 与用户进行交互和沟通。
- 所有回复内容、解释说明、问题确认均使用中文。

## Git 提交限制

- **严禁**在未获得用户明确指令的情况下执行 `git commit` 或 `git push`。
- 所有 Git 变更必须先征得用户确认，不得主动提交。
- 仅当用户明确说出「提交」「commit」「push」「推送」等指令时才可执行。

## Git 提交规范

- Git Commit Message 必须使用 **中文** 编写。
- 说明需清晰简洁，概括本次变更的核心内容。

## 文件变更清单

- 每次修改、创建或删除文件后，必须在回复末尾列出本次操作涉及的 **文件清单**。
- 清单格式：仅列文件路径，不写变更点说明，保持整洁。
