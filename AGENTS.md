# Q-Caliper 铁律

## 1. 交互与环境约束 (Interaction & Environment)

- **语言强制：** 在任何情况下，必须且仅使用 **中文** 进行交互（包括解释、确认及建议）。
- **输出规范：** 每次文件操作（修改/创建/删除）后，必须在回复末尾列出简洁的 [文件路径清单]，无需变更点说明。

## 2. Git 提交协议 (Git Protocol)

- **确认制：** 严禁在未获得用户明确指令的情况下执行 `git commit` 或 `git push`。
- **提交规范：** - 仅当用户说出「提交」、「commit」或「push」时方可执行。
  - Git Commit Message 必须使用 **中文** 编写，说明需清晰简洁。

## 3. 防御性编程逻辑 (Defensive Coding Logic)

- **外科手术式改动 (Surgical Changes)：**
  - **最小触碰：** 仅修改实现需求所**绝对必须**的代码。严禁“顺手”改进相邻代码的注释、格式或命名。
  - **禁止无故重构：** 只要现有代码逻辑未损坏且能运行，严禁进行任何非必要的重构或风格调整。
  - **风格对齐：** 必须完全匹配现有代码的缩进、命名习惯。
  - **清理责任：** 仅清理因本次改动而失效的 import/变量。禁止清理原有的无关死代码。
- **先思考，后编码 (Think Before Coding)：**
  - **陈述假设：** 动手前必须明确陈述你的理解和假设。存在歧义时，必须询问而非盲猜。
  - **困惑即停：** 遇到任何不清晰的逻辑点，立即停止并提问。
- **简约原则：**
  - **拒绝过度工程：** 严禁添加未要求的“灵活性”或“扩展性”。
  - **重写逻辑：** 若 50 行能解决的问题写了 200 行，必须重构简化。
